# Target descriptor: MIPS 64-bit big-endian, Linux glibc, n64 ABI.
# See mipsel.env for the variable contract.

ARCH=mips64
TRIPLE=mips64-linux-gnuabi64
QEMU=qemu-mips64-static
CFLAGS="-O2 -DSSF_CONFIG_HAVE_OPENSSL=0"
LDFLAGS="-lm -lpthread -static"
